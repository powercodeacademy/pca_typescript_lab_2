// Create three arrays: colors (strings), ages (numbers), and students (objects)

type Student = {
  name: string
  grade: number
}

const colors: string[] = ["green", "purple", "teal"]

const ages: number[] = [15, 30, 45]

const students: Student[] = [
  { name: "Liz", grade: 100 }
  { name: "Dani", grade: 100 }
]

// Your code here 👇
